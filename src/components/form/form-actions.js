/** @jsxImportSource theme-ui */

const FormActions = props => {
  return (
    <div
      sx={{
        gridColumn: '1 / span 2',
        marginTop: '2px',
      }}
      {...props}
    />
  )
}

export default FormActions
